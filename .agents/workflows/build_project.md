---
description: How to build and compile the project
---

# Build Project

This project is a static web application built with Baseline (HTML, CSS, JS). 
It does not require any compilation or build steps (`npm install`, `build`, etc. are not needed). 

Your code changes to HTML, CSS, and JS files will take effect immediately.
